box:
  id: python:2.7-slim
  ports:
    - "5000"

dev:
  steps:
    - pip-install
    - internal/watch:
        code: python app.py
        reload: true
        
# Build definition
build:
  # The steps that will be executed on build
  steps:
    # A step that executes `pip install` command
    - pip-install

    # A custom script step, name value is used in the UI
    # and the code value contains the command that get executed
    - script:
        name: python unit test
        code: |
          python app_test.py
          
    - internal/docker-build: 
        dockerfile: Dockerfile 
        image-name: wercker-demo-app  # name used to refer to this image until it's pushed   
    - internal/docker-run:
        image: wercker-demo-app
        name: demo-app-cicd 
    - internal/docker-kill:
        name: demo-app-cicd

python-sample-app-build:
  steps:
    - internal/docker-push:
        username: gse00014528/api.user
        password: zA(nw>J.sl3(6;.o8>Hy
        tag: latest
        repository: gse00014528/axisdemo
        registry: https://iad.ocir.io
